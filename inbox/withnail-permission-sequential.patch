diff --git a/inbox/run_agent.py b/inbox/run_agent.py
index 8e4c0f0..c3f2a1b 100755
--- a/inbox/run_agent.py
+++ b/inbox/run_agent.py
@@ -4650,8 +4650,23 @@ class AIAgent:
                     messages.append(skip_msg)
                 break

-            function_name = tool_call.function.name
+            function_name = tool_call.function.name

+            # ====================== WITHNAIL PERMISSION CHECK ======================
+            if not is_tool_allowed(function_name):
+                error_msg = f"[PERMISSION DENIED] Tool '{function_name}' is not in the Withnail whitelist."
+                print(f"  {error_msg}")
+
+                messages.append({
+                    "role": "tool",
+                    "content": error_msg,
+                    "tool_call_id": tool_call.id,
+                })
+                continue  # Skip this tool (no execution, no side effects)
+            # =====================================================================
+
             # Reset nudge counters when the relevant tool is actually used
             if function_name == "memory":
                 self._turns_since_memory = 0
